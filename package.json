{
  "name": "rss2tlg",
  "version": "0.5.0",
  "description": "Send RSS feed to Telegram channel",
  "scripts": {
    "watch": "shadow-cljs watch app",
    "compile": "shadow-cljs compile app",
    "release": "shadow-cljs release app",
    "clean": "rm -rf out/dist/*",
    "dev": "yarn html && yarn watch",
    "repl": "shadow-cljs node-repl app",
    "build": "yarn clean && yarn release && chmod +x out/dist/rss2tlg.js",
    "server": "shadow-cljs server"
  },
  "devDependencies": {
    "http-server": "0.11.1",
    "shadow-cljs": "2.8.35"
  },
  "author": "i@maxp.dev",
  "license": "EPL-2.0",
  "private": true,
  "dependencies": {
    "node-http-xhr": "1.3.4",
    "sax": "1.2.4"
  },
  "engines": {
    "node": "~10",
    "yarn": "~1"
  }
}
