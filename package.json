{
  "name": "rss2telegram",
  "version": "0.2.0",
  "description": "",
  "scripts": {
    "watch": "shadow-cljs watch app",
    "compile": "shadow-cljs compile app",
    "release": "shadow-cljs release app",
    "clean": "rm -rf out/dist/*",
    "dev": "yarn html && yarn watch",
    "repl": "shadow-cljs node-repl app",
    "build": "yarn clean && yarn release && chmod +x out/dist/rss2telegram.js",
    "server": "shadow-cljs server"
  },
  "devDependencies": {
    "http-server": "0.11.1",
    "shadow-cljs": "2.8.31"
  },
  "author": "i@maxp.dev",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "node-http-xhr": "1.3.4",
    "sax": "1.2.4"
  },
  "engines": {
    "node": "~10",
    "yarn": "~1"
  }
}
